fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pandas==2.2.3
numpy==2.2.1
pyarrow==18.1.0
openpyxl==3.1.5
PyYAML==6.0.2
reportlab==4.2.5
scipy==1.14.1
scikit-learn==1.6.0
pytest==8.3.4
pytest-cov==6.0.0
httpx==0.28.1

# Test-time only: extracts real text from generated PDFs so tests can assert on
# actual report content, not just file size. The app itself never reads PDFs.
pypdf==5.1.0
