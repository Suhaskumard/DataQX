# Base URL of the DataQX backend API (no trailing slash).
# Local dev default (used when this var is unset): http://localhost:8000
# Production (Vercel): set this to your Render backend URL, e.g.
# https://dataqx-backend.onrender.com
VITE_API_BASE_URL=http://localhost:8000
